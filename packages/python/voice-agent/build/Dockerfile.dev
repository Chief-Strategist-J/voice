FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY src/ ./src/
COPY pyproject.toml .
RUN pip install -e .

EXPOSE 8000

CMD ["uvicorn", "src.api.rest.v1.server:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
