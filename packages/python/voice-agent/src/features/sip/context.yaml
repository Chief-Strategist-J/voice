name: sip
description: SIP telephony voice agent (OpenAI STT -> GPT-4o LLM -> OpenAI TTS + Silero VAD)
criticality: high
dependencies:
  - videosdk-plugins-openai
  - videosdk-plugins-silero
  - python-dotenv
