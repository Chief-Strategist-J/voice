Layer 7 — Application Layer
  └── dApps, APIs, SDKs, developer tooling

Layer 6 — Smart Contract Layer
  └── Multi-VM execution (EVM + WASM + Custom VM)

Layer 5 — L2 Rollup Layer
  └── Optimistic rollup + ZK rollup capability
  └── Settles proofs to L1

Layer 4 — L1 Execution Layer
  └── Transaction processing, state transition
  └── Block production

Layer 3 — Consensus Layer
  └── Modular: PoA + PoS + DPoS
  └── Validator set management
  └── Finality mechanism

Layer 2 — P2P Network Layer
  └── Node discovery, gossip protocol
  └── Block + transaction propagation

Layer 1 — Infrastructure Layer
  └── Node hardware, storage, networking
  └── Validator key management
  └── Geographic distribution